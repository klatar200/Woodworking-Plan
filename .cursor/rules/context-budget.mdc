---
description: Token budget — open only the canonical file you need
alwaysApply: true
---

# Context budget

Canonical sources already apply via root `AGENTS.md` / `CLAUDE.md`. Do **not** re-read whole manuals unless stuck.

| Need | Open |
|---|---|
| What to build / scope | `BUSINESS_PLAN.md` (current § only) |
| Sprint status / DoD / score | `BUILD_PLAN.md` §4–§7 |
| Settled business fact | `DECISIONS_LOG.md` (grep settled index first) |
| Cursor VM boot / DB / Clerk | `AGENTS.md` |
| Design tokens / CSS traps | `DESIGN_BRIEF.md` (only when editing UI) |
| Deploy / migrate / backfill | `DEPLOYMENT.md` (only when shipping schema) |

**Do not open by default:** `SPRINT_LOG.md`, `FUTURE_IDEAS.md`, full `DECISIONS_LOG`, `content/plans/**` bulk, draft plans.

**Hard stops** (escalate; don't implement): money/billing/ads/affiliate, paid vendors, legal, branding/public copy, anything in `FUTURE_IDEAS.md`, features absent from `BUSINESS_PLAN` / current `BUILD_PLAN` §4.

**Settled — never re-raise:** live≠launched; Hobby + $0/no monetization; credential rotation deferred; shared Neon `sparkling-band` in dev; dark orange palette intentional.

**Cloud Agent git:** branch `cursor/<name>-5c2e` + PR → `main`. (Local/Keagan trunk-based to `main` per `BUILD_PLAN`.)
